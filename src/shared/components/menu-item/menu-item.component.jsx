import { Link } from "react-router-dom";

const MenuItem = ({ title, collections }) => {
    return (
        <div className="menu-item">
            <h2 className="title">{title}</h2>
                <ul>
                    {
                        // collections.map(collection => )
                    }
                </ul>
        </div>
    );
};

export default MenuItem;